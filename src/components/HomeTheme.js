import { createTheme} from '@mui/material/styles';

const HomeTheme = createTheme({
    palette: {
        primary: {
          main: '#a81310', 
        },
        background: {
          default: '#25344c',
        },
      },
    });

export default HomeTheme;